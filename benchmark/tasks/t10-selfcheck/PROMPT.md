Create versions.py with a function sort_versions(versions: list[str]) -> list[str] that
sorts version strings ascending by THIS project's ordering rules. The included test.py is
the authoritative spec — make it pass. DONE-WHEN: `python3 test.py` prints PASS and exits 0.
