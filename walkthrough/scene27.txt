This capstone is our own service measured against Cotal — competitor against competitor. Cotal connects harnesses: whole agent products, each a separate process with an identity on the mesh. Our MCP servers work one layer deeper, injecting models straight into a single agent's loop, reaching where a mesh structurally can't — any model in the catalog, even ones no harness wraps. Two rivals, two layers, scored on the same tasks.
